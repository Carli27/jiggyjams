import React from "react";

export default function JamProfile(currentJamProfile) {
  // type rfc to create the start of the file
  // below breaks the code
  // const [JamProfile, setJamProfile] = useState([]);

  // don't know how I can apply to cards
  return <div className="container"></div>;
}
